[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "HARDN"
version = "1.0.0"
dependencies = [
    "pexpect",
    "PyYAML==3.13",
    "cython",
]

[tool.setuptools.packages.find]
where = ["."]