#!/bin/sh
set -e

# Simple post-installation script
if [ -f /usr/bin/hardn-xdr ]; then
  chmod +x /usr/bin/hardn-xdr
fi

echo "HARDN-XDR installed successfully. Run it with: hardn-xdr --help"

exit 0
